import PODraftReviewClient from './PODraftReviewClient';

export default function PODraftReviewPage({ params, searchParams }: { params: any; searchParams: any }) {
  return <PODraftReviewClient params={params} searchParams={searchParams} />;
}
