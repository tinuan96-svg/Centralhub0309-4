'use client';
import { useState } from 'react';
import { supabase } from '@/lib/supabase';
import { Card } from '@/lib/design-system';
export default function ManualReserveClient(){const [name,setName]=useState('');const [message,setMessage]=useState('');const [saving,setSaving]=useState(false);const create=async()=>{if(!name.trim())return;setSaving(true);setMessage('');const {error}=await supabase.rpc('create_manual_reserve',{p_name:name.trim()});if(error)setMessage(error.message);else{setMessage('Reserve created.');setName('')}setSaving(false)};return <Card className="p-6 border-slate-800 bg-slate-900/50"><h2 className="text-lg font-black uppercase text-white">Create Manual Reserve</h2><p className="text-xs text-slate-500 mt-1">Create an additional earmarked fund for future planning.</p><div className="flex flex-col sm:flex-row gap-3 mt-5"><input value={name} onChange={e=>setName(e.target.value)} placeholder="e.g. Tax Reserve" className="flex-1 rounded-xl bg-slate-950 border border-slate-800 px-4 py-3 text-sm text-white outline-none"/><button disabled={saving||!name.trim()} onClick={create} className="rounded-xl bg-cyan-500 px-5 py-3 text-xs font-black uppercase text-slate-950 disabled:opacity-40">{saving?'Creating…':'Create Reserve'}</button></div>{message&&<p className="text-xs text-slate-400 mt-3">{message}</p>}</Card>}
