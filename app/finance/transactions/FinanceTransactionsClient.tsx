'use client';

import { useCallback, useEffect, useMemo, useState } from 'react';
import Link from 'next/link';
import { useSearchParams } from 'next/navigation';
import { supabase } from '@/lib/supabase';
import { formatCurrency } from '@/lib/utils/currency';

type Tx = {
  id: string; transaction_date: string; description: string; amount: number; type: 'credit'|'debit';
  balance?: number | null; reference?: string | null; merchant?: string | null;
  transaction_category?: string | null; accounting_category?: string | null;
  classification_status?: string | null; reconciliation_status?: string | null;
  is_reconciled?: boolean; reconciliation_notes?: string | null; notes?: string | null;
};

type PendingRule = { sourceId: string; category: string; accounting: string; sourceText: string; matches: Tx[] };

const categories = [
  ['sales_income','Sales income','revenue'], ['supplier_payment','Supplier payment / COGS','cogs'],
  ['operating_expense','Operating expense','operating_expense'], ['tax','Tax','tax'],
  ['refund','Refund','other'], ['transfer','Bank transfer','transfer'],
  ['savings_allocation','Savings allocation / earmarked funds','transfer'],
  ['financing','Finance / loan','finance_cost'], ['other_income','Other income','revenue'],
  ['other_expense','Other expense','other'], ['unknown','Needs review','other'],
] as const;

const normalize = (value: unknown) => String(value || '').toLowerCase().replace(/[^a-z0-9]+/g, ' ').trim().replace(/\s+/g, ' ');
const matchKeys = (tx: Tx) => Array.from(new Set([normalize(tx.description), normalize(tx.merchant), normalize(tx.reference)].filter(Boolean)));

export default function FinanceTransactionsClient() {
  const searchParams = useSearchParams();
  const requestedStatus = searchParams.get('status');
  const [rows,setRows]=useState<Tx[]>([]); const [loading,setLoading]=useState(true); const [saving,setSaving]=useState<string|null>(null); const [bulkSaving,setBulkSaving]=useState(false); const [reconciling,setReconciling]=useState<string|null>(null); const [error,setError]=useState<string|null>(null); const [pending,setPending]=useState<PendingRule|null>(null); const [view,setView]=useState<'unreconciled'|'all'|'reconciled'>(requestedStatus==='reconciled'?'reconciled':'unreconciled');
  const load=useCallback(async()=>{setLoading(true);setError(null);const {data,error}=await supabase.from('bank_transactions').select('*').order('transaction_date',{ascending:false}).order('created_at',{ascending:false}).limit(1000);if(error)setError(error.message);setRows(((data||[]) as any[]).map(r=>({...r,is_reconciled:r.is_reconciled===true})) as Tx[]);setLoading(false);setPending(null);},[]);
  useEffect(()=>{load();},[load]);

  const unreconciled=rows.filter(x=>!x.is_reconciled && x.reconciliation_status!=='reconciled');
  const reconciled=rows.filter(x=>x.is_reconciled || x.reconciliation_status==='reconciled');
  const visibleRows=useMemo(()=>view==='unreconciled'?unreconciled:view==='reconciled'?reconciled:rows,[view,rows]);
  const review=visibleRows.filter(x=>!x.transaction_category||x.transaction_category==='unknown'||x.classification_status==='needs_review').length;
  const previewMatches=useMemo(()=>pending?.matches||[],[pending]);

  const prepareClassification=(tx:Tx,category:string,accounting:string)=>{if(category==='unknown'){setPending(null);return;}const sourceKeys=matchKeys(tx);const sourceIndex=rows.findIndex(x=>x.id===tx.id);const matches=rows.filter((x,i)=>i>sourceIndex&&!x.is_reconciled&&x.reconciliation_status!=='reconciled'&&(x.classification_status==='needs_review'||!x.transaction_category||x.transaction_category==='unknown')&&matchKeys(x).some(k=>sourceKeys.includes(k)));setPending({sourceId:tx.id,category,accounting,sourceText:tx.description||tx.merchant||tx.reference||tx.id,matches});};
  const classifyOne=async(tx:Tx,category:string,accounting:string)=>{setSaving(tx.id);setError(null);const {error}=await supabase.rpc('classify_financial_transaction',{p_transaction_id:tx.id,p_transaction_category:category,p_accounting_category:accounting,p_notes:tx.notes||null});if(error)setError(error.message);else setRows(r=>r.map(x=>x.id===tx.id?{...x,transaction_category:category==='savings_allocation'?'transfer':category,accounting_category:accounting,classification_status:category==='unknown'?'needs_review':'classified'}:x));setSaving(null);};
  const applyPending=async()=>{if(!pending)return;const targets=[pending.sourceId,...pending.matches.map(x=>x.id)];setBulkSaving(true);setError(null);const results=await Promise.all(targets.map(id=>supabase.rpc('classify_financial_transaction',{p_transaction_id:id,p_transaction_category:pending.category,p_accounting_category:pending.accounting,p_notes:null})));const failed=results.find(x=>x.error)?.error;if(failed){setError(failed.message);setBulkSaving(false);return;}setRows(r=>r.map(x=>targets.includes(x.id)?{...x,transaction_category:pending.category==='savings_allocation'?'transfer':pending.category,accounting_category:pending.accounting,classification_status:'classified'}:x));setPending(null);setBulkSaving(false);};
  const reconcile=async(tx:Tx)=>{setReconciling(tx.id);setError(null);const {error}=await supabase.rpc('reconcile_bank_transaction',{p_transaction_id:tx.id,p_notes:'Reconciled from Bank Reconciliation centre'});if(error)setError(error.message);else setRows(r=>r.map(x=>x.id===tx.id?{...x,is_reconciled:true,reconciliation_status:'reconciled',reconciliation_notes:'Reconciled from Bank Reconciliation centre'}:x));setReconciling(null);};

  return <main className="p-4 sm:p-6 space-y-6 max-w-[1700px] mx-auto"><header className="flex flex-col lg:flex-row lg:items-end lg:justify-between gap-4"><div><p className="text-cyan-400 text-[10px] font-black uppercase tracking-[.25em]">CentralHub Finance</p><h1 className="text-2xl sm:text-3xl font-black text-white">Bank Reconciliation</h1><p className="text-sm text-slate-500 mt-1">Reconcile every bank movement separately from its accounting classification. Only reconciled transactions are considered complete.</p></div><div className="flex gap-2"><Link href="/finance" className="px-4 py-2 rounded-xl bg-slate-800 border border-slate-700 text-white text-[10px] font-black uppercase tracking-widest">Finance</Link><button onClick={load} className="px-4 py-2 rounded-xl bg-slate-800 border border-slate-700 text-white text-[10px] font-black uppercase tracking-widest">Refresh</button></div></header><div className="rounded-2xl border border-cyan-500/20 bg-cyan-500/5 p-4 text-xs text-slate-300"><span className="font-black text-cyan-300">How reconciliation works:</span> classify the movement, verify what it represents, then click <b>Reconcile</b>. A classified transaction is still outstanding until it is reconciled. No physical cash is modelled.</div>{error&&<div className="rounded-2xl border border-rose-500/30 bg-rose-500/10 p-4 text-sm text-rose-300">{error}</div>}<section className="grid grid-cols-2 lg:grid-cols-4 gap-3"><div className="bg-slate-900 border border-slate-800 rounded-2xl p-4"><p className="text-[10px] text-slate-500 uppercase font-black">Total bank movements</p><p className="text-2xl font-black text-white">{rows.length}</p></div><button onClick={()=>setView('unreconciled')} className={`text-left bg-slate-900 border rounded-2xl p-4 ${view==='unreconciled'?'border-amber-400/50':'border-slate-800'}`}><p className="text-[10px] text-slate-500 uppercase font-black">Left to reconcile</p><p className={`text-2xl font-black ${unreconciled.length?'text-amber-300':'text-emerald-300'}`}>{unreconciled.length}</p></button><button onClick={()=>setView('reconciled')} className={`text-left bg-slate-900 border rounded-2xl p-4 ${view==='reconciled'?'border-emerald-400/50':'border-slate-800'}`}><p className="text-[10px] text-slate-500 uppercase font-black">Reconciled</p><p className="text-2xl font-black text-emerald-300">{reconciled.length}</p></button><div className="bg-slate-900 border border-slate-800 rounded-2xl p-4"><p className="text-[10px] text-slate-500 uppercase font-black">Outstanding value</p><p className="text-2xl font-black text-rose-300">{formatCurrency(unreconciled.reduce((a,x)=>a+Math.abs(Number(x.amount||0)),0))}</p></div></section><div className="flex flex-wrap items-center gap-2"><button onClick={()=>setView('unreconciled')} className={`px-4 py-2 rounded-xl text-[10px] font-black uppercase tracking-widest ${view==='unreconciled'?'bg-amber-400 text-slate-950':'bg-slate-900 text-slate-400 border border-slate-800'}`}>Unreconciled ({unreconciled.length})</button><button onClick={()=>setView('all')} className={`px-4 py-2 rounded-xl text-[10px] font-black uppercase tracking-widest ${view==='all'?'bg-cyan-400 text-slate-950':'bg-slate-900 text-slate-400 border border-slate-800'}`}>All ({rows.length})</button><button onClick={()=>setView('reconciled')} className={`px-4 py-2 rounded-xl text-[10px] font-black uppercase tracking-widest ${view==='reconciled'?'bg-emerald-400 text-slate-950':'bg-slate-900 text-slate-400 border border-slate-800'}`}>Reconciled ({reconciled.length})</button><span className="ml-auto text-[10px] uppercase tracking-widest text-slate-500">{review} need classification in this view</span></div>{pending&&<section className="rounded-3xl border border-cyan-500/30 bg-cyan-500/5 p-5 space-y-4"><div className="flex flex-col lg:flex-row lg:items-start lg:justify-between gap-4"><div><p className="text-[10px] font-black uppercase tracking-widest text-cyan-300">Matching transactions found</p><h2 className="text-lg font-black text-white mt-1">Apply “{categories.find(c=>c[0]===pending.category)?.[1]}” to repeated transactions?</h2><p className="text-xs text-slate-400 mt-1">These are still unreconciled and match the same normalized name, description, merchant or reference. Nothing is saved until you click Apply.</p></div><div className="flex gap-2"><button disabled={bulkSaving} onClick={()=>{const source=rows.find(x=>x.id===pending.sourceId);if(source)classifyOne(source,pending.category,pending.accounting);setPending(null);}} className="px-4 py-2 rounded-xl bg-slate-800 border border-slate-700 text-white text-[10px] font-black uppercase tracking-widest">Selected only</button><button disabled={bulkSaving} onClick={applyPending} className="px-4 py-2 rounded-xl bg-cyan-500 text-slate-950 text-[10px] font-black uppercase tracking-widest">{bulkSaving?'Applying…':`Apply to ${previewMatches.length+1} transactions`}</button><button disabled={bulkSaving} onClick={()=>setPending(null)} className="px-3 py-2 rounded-xl text-slate-400 text-[10px] font-black uppercase tracking-widest">Cancel</button></div></div><div className="grid gap-2 max-h-64 overflow-auto">{[rows.find(x=>x.id===pending.sourceId),...previewMatches].filter(Boolean).map((tx:any)=><div key={tx.id} className="flex items-center justify-between rounded-xl bg-slate-950/60 border border-slate-800 px-3 py-2"><div><span className="text-xs font-bold text-white">{tx.description||tx.merchant||'Transaction'}</span><span className="text-[10px] text-slate-500 ml-3">{new Date(tx.transaction_date).toLocaleDateString('en-GB')}</span></div><span className={`text-xs font-black ${tx.type==='credit'?'text-emerald-300':'text-rose-300'}`}>{tx.type==='credit'?'+':'-'}{formatCurrency(Number(tx.amount||0))}</span></div>)}</div></section>}<section className="bg-slate-900 border border-slate-800 rounded-3xl overflow-hidden"><div className="overflow-x-auto"><table className="w-full text-sm"><thead className="bg-slate-800/50 text-[9px] uppercase tracking-widest text-slate-500"><tr><th className="p-4 text-left">Date</th><th className="p-4 text-left">Transaction</th><th className="p-4 text-right">Amount</th><th className="p-4 text-left">Classification</th><th className="p-4 text-left">Accounting</th><th className="p-4 text-center">Reconciliation</th><th className="p-4 text-right">Action</th></tr></thead><tbody className="divide-y divide-slate-800">{visibleRows.map(tx=><tr key={tx.id} className={pending?.matches.some(x=>x.id===tx.id)?'bg-cyan-500/5':''}><td className="p-4 text-slate-400 whitespace-nowrap">{new Date(tx.transaction_date).toLocaleDateString('en-GB')}</td><td className="p-4"><p className="font-bold text-white">{tx.description}</p><p className="text-[10px] text-slate-500">{tx.merchant||tx.reference||''}</p></td><td className={`p-4 text-right font-black ${tx.type==='credit'?'text-emerald-300':'text-rose-300'}`}>{tx.type==='credit'?'+':'-'}{formatCurrency(Number(tx.amount||0))}</td><td className="p-4"><select disabled={saving===tx.id||bulkSaving||tx.is_reconciled} value={tx.transaction_category||'unknown'} onChange={e=>{const c=categories.find(x=>x[0]===e.target.value)||categories[categories.length-1];prepareClassification(tx,c[0],c[2]);}} className="bg-slate-950 border border-slate-700 rounded-lg px-2 py-2 text-xs text-white">{categories.map(c=><option key={c[0]} value={c[0]}>{c[1]}</option>)}</select></td><td className="p-4 text-xs text-slate-400">{tx.accounting_category||'—'}</td><td className="p-4 text-center"><span className={`px-2 py-1 rounded-lg text-[8px] font-black uppercase ${tx.is_reconciled||tx.reconciliation_status==='reconciled'?'bg-emerald-500/10 text-emerald-300':'bg-amber-500/10 text-amber-300'}`}>{tx.is_reconciled||tx.reconciliation_status==='reconciled'?'RECONCILED':'OUTSTANDING'}</span></td><td className="p-4 text-right">{tx.is_reconciled||tx.reconciliation_status==='reconciled'?<span className="text-[9px] uppercase tracking-widest text-emerald-300 font-black">Complete</span>:<button disabled={reconciling===tx.id||bulkSaving} onClick={()=>reconcile(tx)} className="px-3 py-2 rounded-lg bg-emerald-500 text-slate-950 text-[9px] font-black uppercase tracking-widest">{reconciling===tx.id?'Reconciling…':'Reconcile'}</button>}</td></tr>)}</tbody></table>{loading&&<div className="p-12 text-center text-slate-500">Loading bank movements…</div>}{!loading&&!visibleRows.length&&<div className="p-12 text-center text-emerald-300 font-bold">Nothing left to reconcile in this view.</div>}</div></section></main>;
}
